{#-
  expo-metro-config.tera — apps/mobile/metro.config.js

  Metro resolver config that lets the Expo shell consume the
  ggen-manufactured ../../packages/* tree from outside its own node_modules.
-#}
// ============================================================
// DO NOT EDIT — generated by ggen for {{ appSlug }}
// Source: .specify/templates/zoela/expo-metro-config.tera
// ============================================================

const { getDefaultConfig } = require('expo/metro-config');
const path = require('path');

const projectRoot = __dirname;
const workspaceRoot = path.resolve(projectRoot, '../..');

const config = getDefaultConfig(projectRoot);

config.watchFolders = [workspaceRoot];

config.resolver.nodeModulesPaths = [
  path.resolve(projectRoot, 'node_modules'),
  path.resolve(workspaceRoot, 'node_modules'),
];

module.exports = config;
