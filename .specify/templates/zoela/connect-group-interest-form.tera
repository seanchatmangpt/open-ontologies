// Generated by CodeManufactory — do not edit
// Source: ontology/zoela/connect-group-routes.ttl

import React, { useState } from 'react';
import { View, Text, TextInput, TouchableOpacity, StyleSheet } from 'react-native';

{% set entry_codes = stage_codes | split(pat="|") %}
{% set entry_labels = stage_labels | split(pat="|") %}

// Entry stage: {{ entry_labels[0] }}
// OCEL event on submit: {{ ocel_event_types | split(pat="|") | first }}

export interface GroupInterestFormData {
  schedulePreference: string;
  locationPreference: 'in-person' | 'online' | 'hybrid';
  notes: string;
}

export function ConnectGroupInterestForm({
  onSubmit,
  onCancel,
}: {
  onSubmit: (data: GroupInterestFormData) => void;
  onCancel: () => void;
}) {
  const [schedule, setSchedule] = useState('');
  const [location, setLocation] = useState<'in-person' | 'online' | 'hybrid'>('in-person');
  const [notes, setNotes] = useState('');

  return (
    <View style={styles.container}>
      <Text style={styles.title}>Join a Connect Group</Text>
      <Text style={styles.label}>Schedule Preference</Text>
      <TextInput
        style={styles.input}
        value={schedule}
        onChangeText={setSchedule}
        placeholder="e.g. Sunday evenings, weekday mornings"
      />
      <Text style={styles.label}>Location Mode</Text>
      {(['in-person', 'online', 'hybrid'] as const).map(mode => (
        <TouchableOpacity
          key={mode}
          style={[styles.option, location === mode && styles.selected]}
          onPress={() => setLocation(mode)}
        >
          <Text>{mode}</Text>
        </TouchableOpacity>
      ))}
      <Text style={styles.label}>Additional Notes</Text>
      <TextInput
        style={[styles.input, styles.multiline]}
        value={notes}
        onChangeText={setNotes}
        multiline
        placeholder="Anything else we should know?"
      />
      <TouchableOpacity
        style={styles.submit}
        onPress={() => onSubmit({ schedulePreference: schedule, locationPreference: location, notes })}
      >
        <Text style={styles.submitText}>Express Interest</Text>
      </TouchableOpacity>
      <TouchableOpacity style={styles.cancel} onPress={onCancel}>
        <Text>Cancel</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { padding: 16 },
  title: { fontSize: 20, fontWeight: 'bold', marginBottom: 16 },
  label: { fontSize: 14, fontWeight: '600', marginTop: 12, marginBottom: 4 },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 8 },
  multiline: { height: 80, textAlignVertical: 'top' },
  option: { padding: 8, borderWidth: 1, borderColor: '#ccc', borderRadius: 8, marginBottom: 4 },
  selected: { backgroundColor: '#e0f0ff', borderColor: '#0070f3' },
  submit: { backgroundColor: '#0070f3', padding: 16, borderRadius: 8, alignItems: 'center', marginTop: 16 },
  submitText: { color: 'white', fontWeight: 'bold' },
  cancel: { alignItems: 'center', padding: 12 },
});
