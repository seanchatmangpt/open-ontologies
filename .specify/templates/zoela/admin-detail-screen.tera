{#-
  admin-detail-screen.tera — Generated admin detail screen component for ZOE LA Mobile

  Generated by ggen from ontology/zoela/navigation.ttl
  Do NOT edit — regenerate with: ggen sync --rule zoela-admin-detail

  ggen v5 per-row rendering model: renders once per SELECT row.
  extract-screens.rq returns one row per AppScreen.
  Template only emits a component when is_admin_only == "true".

  SPARQL variables available directly (no "rows" wrapper):
    screen_id           — skos:notation (e.g. "admin_open_needs")
    screen_label        — rdfs:label (e.g. "Admin: Open Needs")
    nav_route           — navigation route identifier
    parent_tab_code     — parent tab code (default: "HOME")
    screen_stack        — navigation stack (default: "MainStack")
    required_role       — minimum role code (default: "MEMBER")
    primary_object_type — object type this screen displays
    primary_route_category — service route category
    is_admin_only       — "true" / "false"
    screen_order        — display order integer string
-#}
// Generated by CodeManufactory — do not edit
// Source: ontology/zoela/navigation.ttl + extract-admin-screens.rq
// Regenerate with: ggen sync --rule zoela-admin-detail

import React from 'react';
import { ScrollView, Text, View, StyleSheet } from 'react-native';

// Admin Detail: {{ screen_label }}
// Object type:  {{ primary_object_type }}
// Route:        {{ nav_route | default(value=screen_id) }}
export function {{ nav_route | default(value=screen_id) | replace(from="/", to="_") | replace(from="-", to="_") }}DetailScreen() {
  return (
    <ScrollView style={styles.container}>
      <Text style={styles.title}>{{ screen_label }}</Text>
      {/* Tab: {{ parent_tab_code }} */}
      {/* Stack: {{ screen_stack }} */}
      {/* Required role: {{ required_role }} */}
      {/* Object type: {{ primary_object_type }} */}
      <View style={styles.content}>
        <Text style={styles.placeholder}>
          {{ primary_object_type }} detail view
        </Text>
      </View>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container:   { flex: 1, backgroundColor: '#fff' },
  content:     { padding: 16 },
  title:       { fontSize: 22, fontWeight: '700', paddingHorizontal: 16, paddingTop: 16, marginBottom: 8 },
  placeholder: { fontSize: 14, color: '#666', fontStyle: 'italic' },
});
