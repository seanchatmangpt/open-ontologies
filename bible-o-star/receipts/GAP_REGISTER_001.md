# GAP_REGISTER_001 — bible-o-star / open-ontologies

**Generated:** 2026-06-02
**Branch:** ontostar-integration
**Honest Re-verdict:** BLOCKED
**One-line verdict:** Every cargo invocation fails before a single test runs because open-ontologies has no rust-toolchain.toml and stable rustc rejects the nightly-only features in wasm4pm-compat; simultaneously, the EARL certification receipt is a hardcoded stub, the primary ontology file is corrupted with SHACL output text, and two of four BLAKE3 receipt hashes do not match actual files — make cell8-certify cannot run, and even if it could, it would certify nothing.

---

## Ranked Gap Table

### CRITICAL

| ID | Description | Evidence | Fix | Blocks ALIVE |
|----|-------------|----------|-----|--------------|
| GAP_001 | No rust-toolchain.toml in /Users/sac/open-ontologies. Active toolchain is stable 1.95.0. wasm4pm-compat unconditionally declares #![feature(generic_const_exprs)] and four other nightly-only features. Every cargo invocation fails immediately with E0554. | `cargo build` → `error[E0554]: #![feature] may not be used on the stable release channel --> /Users/sac/wasm4pm-compat/src/lib.rs:148` | Add /Users/sac/open-ontologies/rust-toolchain.toml with `[toolchain]\nchannel = "nightly"`. Nightly is already installed (nightly-aarch64-apple-darwin present). | YES |
| GAP_002 | wasm4pm-compat is an unconditional dependency of wasm4pm (line 35 of wasm4pm/Cargo.toml), making the nightly requirement load-bearing for all open-ontologies builds. There is no cfg-gate or optional feature to disable this path. | Cargo.lock confirms chain: open-ontologies → wasm4pm → wasm4pm-compat v0.1.0 (path = /Users/sac/wasm4pm-compat, features = ["wasm4pm"]). Any toolchain other than nightly fails. | Simplest fix is GAP_001 (rust-toolchain.toml). Alternative: gate wasm4pm-compat as optional behind a Cargo feature in wasm4pm/Cargo.toml, but this requires wasm4pm surgery. | YES |
| GAP_003 | ontology/nehemiah-52.ttl is corrupted. The file (811 bytes, 17 lines) contains a plain-text pyshacl Validation Report block instead of the 434-triple Turtle ontology. It is untracked in git so there is no version-control recovery path. | `cat nehemiah-52.ttl` → first line is "Validation Report" not `@prefix`. `wc -l` → 17 lines. `b3sum` → e21876b2... (does not match stored receipt hash f05dd516...). `git status` → `?? bible-o-star/ontology/`. | Reconstruct nehemiah-52.ttl from docs/NEHEMIAH_52_OPERATING_GRAMMAR.md, BIBLE_O_STAR_002/003.md manifests, and shapes files. Declare all 14 domain classes (bos:Gate, bos:Builder, bos:WallSection, bos:Verdict, bos:InspectionReceipt, bos:FalseReport, bos:Mocker, bos:MockerFeedback, bos:MusterLedgerRecord, bos:MusterRegistry, bos:Receipt, bos:UsuryAudit, bos:UsuryLedgerRecord, bos:CourierRecord) and 10 gate named individuals (bos:SheepGate through bos:InspectionGate) each typed as bos:Gate. | YES |
| GAP_004 | BLAKE3 receipt chain broken for 2 of 4 files. receipt-chain.ttl stores: bible-o-star.ttl → 0a421103..., nehemiah-52.ttl → f05dd516.... Actual b3sum results: bible-o-star.ttl → f7f43fb5..., nehemiah-52.ttl → e21876b2.... The Ed25519 SealAssertion signs Receipt_SourceLedger which depends on the chain; the entire chain is invalidated. | `b3sum bible-o-star.ttl` → f7f43fb5...; receipt-chain.ttl stores 0a421103... Mismatch confirmed. `b3sum nehemiah-52.ttl` → e21876b2...; stored f05dd516.... Shapes and source-ledger hashes match. | After reconstructing nehemiah-52.ttl: recompute b3sum for all four files, update receipt-chain.ttl with fresh cell8:receiptHash values, re-seal with new Ed25519 signature over concatenated hashes. | YES |
| GAP_005 | tools/emit-earl-report.py is a 23-line hardcoded stub. It imports only `datetime`, reads zero files, computes zero hashes, verifies zero signatures, checks no OCEL journal, and unconditionally emits earl:passed for all 13 gates regardless of actual test results. The Makefile's cell8-certify target uses this as a proof gate. | `cat tools/emit-earl-report.py` → loop over ["A1".."A13"] unconditionally printing `earl:outcome earl:passed`. No file I/O. No hash verification. No Ed25519 check. | Replace with a real verifier: (a) accept cargo test JSON output (--format json) and map test status to earl:passed/earl:failed, (b) compute b3sum hashes and compare against receipt-chain.ttl, (c) verify Ed25519 SealAssertion, (d) exit non-zero on any failure. | YES |
| GAP_006 | 10 of 15 example TTL files fail SHACL validation (ClassConstraintComponent). SHACL shapes bos:BuilderShape, bos:WallSectionShape, bos:MusterLedgerRecordShape require sh:class bos:Gate on bos:assignedToGate values. Gate individuals have no rdf:type bos:Gate because nehemiah-52.ttl (which defined them) is corrupted. | pyshacl standalone on any wall-section example → "Value does not have class bos:Gate". validate_bible_o_star.sh step 2 crashes with rdflib BadSyntax on nehemiah-52.ttl (0 triples parsed). | Restore nehemiah-52.ttl (GAP_003). Then run: `python3 -m pyshacl -s ontology/nehemiah-52-shapes.ttl -e ontology/nehemiah-52.ttl -d examples/<file>.ttl` for each example. The -e flag is required (not -o). | YES |
| GAP_007 | tools/emit-earl-report.py uses namespace urn:cell8:gate: with gate IRIs GateA1..GateA13, while the Rust emit_earl_report() uses urn:ontostar:gate: with IRIs A1_WorkflowDeclared..A13_ReplayProof. The Python output does not satisfy cell8-conformance-shapes.ttl (which targets urn:ontostar:cell8: prefixed nodes). The namespaces are irreconcilably different. | `grep "cell8:Gate" tools/emit-earl-report.py` → `cell8:GateA1`. `grep "GATE_NAMES" src/cell8.rs` → `A1_WorkflowDeclared`. SHACL shapes use urn:ontostar:gate: prefix. | Align Python script to use canonical namespace urn:ontostar:gate: and gate name strings matching GATE_NAMES in src/cell8.rs (A1_WorkflowDeclared through A13_ReplayProof). Add CI test piping Python output through pyshacl against cell8-conformance-shapes.ttl. | YES |
| GAP_008 | make cell8-certify fails at the adversarial prerequisite which fails at check-dead-params which fails immediately at cargo invocation due to GAP_001 (stable toolchain). verify-setup.sh does not probe for nightly and emits no diagnostic about the toolchain mismatch — it just reports cargo --version without checking for "nightly". | `make check` → fails at `cargo test --test dead_param_gate_test` with E0554. verify-setup.sh step 1: `ok "cargo $(cargo --version)"` → passes with stable 1.95.0 without any warning. | Add toolchain probe to scripts/verify-setup.sh: `if rustc --version | grep -q nightly; then ok "nightly toolchain"; else fail "nightly required (wasm4pm-compat) — add rust-toolchain.toml with channel = nightly"` | YES |
| GAP_009 | cell8_thirteen_gates.rs has no deny-path tests for gates A1 through A8 (WorkflowDeclared, ScopeClosed, OCELComplete, POWLReplayPass, ThresholdPass, RequiredStagesPresent, NoBypassRevocation, ReceiptValid). Only A9-A13 have deny-path test functions. A gate with no deny-path test is an unchecked assertion. | `grep -n "fn a[0-9]" cell8_thirteen_gates.rs` → only a9_provenance_missing_denies, a10_attestation_missing_denies, a11_temporal_skew_denies, a12_dependency_closure_broken_denies, a13_replay_divergence_denies. No a1_ through a8_ deny functions. | Add test functions: a1_workflow_not_declared_denies, a2_scope_not_closed_denies, a3_ocel_incomplete_denies, a4_replay_failed_denies, a5_threshold_failed_denies, a6_required_stage_missing_denies, a7_session_revoked_denies, a8_receipt_invalid_denies. Each must mutate exactly one field of ok_bag() and assert the correct DefectClass. | YES |

### MAJOR

| ID | Description | Evidence | Fix | Blocks ALIVE |
|----|-------------|----------|-----|--------------|
| GAP_010 | 6 dirty files in ontostar-integration branch: Cargo.lock, Makefile, audit.json, receipts/manufacture-receipt.json, tests/integration_ggen_onto_feedback.rs, .ggen/receipts/latest.json. Non-reproducible build state. | `git status` → modified Cargo.lock (not committed). | After fixing GAP_001, commit or stash dirty files. At minimum commit Cargo.lock and Makefile changes so the branch is reproducible. | NO |
| GAP_011 | pyshacl -o flag misuse across all validation scripts. -o in pyshacl 0.31.0 is the OUTPUT FILE flag, not the ontology-loading flag. The correct flag for loading an extra ontology graph is -e / --ont-graph. Any script using -o <ontology.ttl> was writing the report to a file named after the ontology path, not loading the ontology. | `python3 -m pyshacl --help` → `-o OUTPUT, --output OUTPUT: Send output to a file`. `-e ONT, --ont-graph ONT: Load extra ontology from file`. | Replace all `pyshacl -s <shapes> -o <ontology> -d <data>` with `pyshacl -s <shapes> -e <ontology> -d <data>` in scripts, Makefile, and agent scripts. | NO (root cause is GAP_003/006, but this compounds it) |
| GAP_012 | 14 domain classes are missing from all currently-parseable TTL files because nehemiah-52.ttl is corrupted. Classes: bos:Builder, bos:CourierRecord, bos:FalseReport, bos:Gate, bos:InspectionReceipt, bos:Mocker, bos:MockerFeedback, bos:MusterLedgerRecord, bos:MusterRegistry, bos:Receipt, bos:UsuryAudit, bos:UsuryLedgerRecord, bos:Verdict, bos:WallSection. | `rapper -i turtle bible-o-star.ttl -o ntriples | grep "owl:Class"` → none of the 14 classes present in bible-o-star.ttl. All 14 were defined only in nehemiah-52.ttl. | Reconstruct nehemiah-52.ttl (GAP_003). This gap is a consequence of GAP_003 and resolves with it. | NO (resolves with GAP_003) |
| GAP_013 | make adversarial calls expand as a prerequisite (cargo expand --lib > target/expanded.rs). If cargo-expand is absent, the expand step exits 1 and halts adversarial and cell8-certify. The expanded_dispatch_arms_match_source_attributes test silently skips when target/expanded.rs is absent — B4 bypass thesis is not actually proven. | Test code: returns pass when target/expanded.rs is missing (silent skip). Expand step: cargo-expand is optional per expand-prereq check but adversarial depends on expand unconditionally. | Make expanded_dispatch_arms_match_source_attributes hard-fail (not skip) when target/expanded.rs is missing. Add comment documenting absence of expanded.rs is a defect. | NO |
| GAP_014 | check-test-removal-tag gate vacuously passes when run locally on main with no BASE set. Merge-base of HEAD..HEAD is HEAD itself, so zero test removals are counted. A developer removing load-bearing tests and committing to main is never caught. | Script: default BASE computation uses `git merge-base HEAD HEAD` when BASE is not set → equals HEAD → no diff → vacuous pass. | Default BASE to `origin/main` when not set. Add warning when computed BASE equals HEAD. Document LOCAL vs CI behavioral difference in script header. | NO |
| GAP_015 | The A10 (ExternalAttestation) gate has no test for the real Ed25519 verify_strict path. The positive test uses allow_legacy_unsigned=true with signature=None, bypassing all Ed25519 cryptography. Only the missing-signature deny path is tested. | `grep -A10 "fn a10" cell8_thirteen_gates.rs` → test uses `allow_legacy_unsigned: true, signature: None`. No test with a real keypair and verify_strict path. | Add: (a) a10_valid_ed25519_passes — generate real Ed25519 keypair, sign canonical bytes, assert cell_ready succeeds; (b) a10_invalid_ed25519_denies — wrong signature, assert DefectClass::AttestationInvalid. | NO |
| GAP_016 | validate_bible_o_star.sh step 2 crashes at runtime with rdflib BadSyntax on nehemiah-52.ttl (0 triples parsed). The script uses set -e but the pyshacl call is in a python3 -c block where the exception surfaces as a traceback, not a clean failure. Script reports VALIDATION COMPLETE regardless. | Running validate_bible_o_star.sh → BadSyntax traceback at step 2 from nehemiah-52.ttl. Script continues to "=== VALIDATION COMPLETE ===". | Fix nehemiah-52.ttl (GAP_003). Restructure script so Python exceptions cause non-zero exit codes that set -e catches. | NO (resolves with GAP_003) |
| GAP_017 | validate_bible_o_star.sh never checks receipt-chain.ttl: no grep, no parse, no hash verification. The "VALIDATION COMPLETE" claim provides zero evidence the receipt chain is sound. | `cat validate_bible_o_star.sh` → 4 steps; none reference receipt-chain.ttl. | Add step 5: parse receipt-chain.ttl with rapper to extract cell8:subjectPath and cell8:receiptHash pairs; run b3sum on each referenced file; fail if any hash does not match. | NO |
| GAP_018 | make check includes cargo audit which requires network access to fetch the advisory database. In offline environments this step fails or produces stale results. No cached advisory database is present in the repo. | No .cargo/audit.toml or pinned advisory snapshot in repo. cargo audit fetches from github.com/rustsec/advisory-db at runtime. | Add advisory database path to .cargo/config.toml or ship pinned advisory snapshot. Document that make check requires network access for audit step. | NO |

### MINOR

| ID | Description | Evidence | Fix | Blocks ALIVE |
|----|-------------|----------|-----|--------------|
| GAP_019 | nehemiah-52.ttl is untracked in git (even before corruption). No version-control recovery path existed. | `git status` → `?? bible-o-star/ontology/` (entire ontology/ directory is untracked). | After reconstruction, `git add bible-o-star/ontology/nehemiah-52.ttl` and commit. All 26 TTL files should be tracked. | NO |
| GAP_020 | 5 of 15 example files declare inline rdf:type bos:Gate assertions; other examples rely on the ontology graph. Inconsistent self-containedness. | fish-gate-landing-page.ttl, courier-false-report-record.ttl, inspection-gate-receipt.ttl, mocker-feedback-record.ttl each declare bos:*Gate a bos:Gate inline. | Standardize: either all examples rely on -e ontology for class resolution, or all declare inline types. Document the chosen policy. | NO |
| GAP_021 | cell8-conformance-shapes.ttl only checks that exactly 13 c8:hasGate edges exist. It does not validate that all 13 canonical gate IRIs are distinct and present. A report with 13 duplicate A1 gate IRIs would pass SHACL. | Shape: `sh:maxCount 13; sh:minCount 13` on c8:hasGate. No sh:in or sh:pattern constraints on gate IRI values. | Add sh:in constraints or sh:pattern constraints on c8:hasGate objects requiring each of the 13 canonical IRIs exactly once, or a separate shape enumerating all 13 required values. | NO |
| GAP_022 | Active shell toolchain defaults to stable unless cwd is inside /Users/sac/wasm4pm-compat (where that repo's rust-toolchain.toml overrides). Engineers working from open-ontologies/ always see stable as default, masking the nightly requirement until cargo build fails. | `cd /Users/sac/open-ontologies && rustc --version` → 1.95.0 (stable). No diagnostic. | Add rust-toolchain.toml (GAP_001). Optionally add .cargo/config.toml note explaining nightly requirement. | NO |
| GAP_023 | cell8-certify has no explicit python3 availability check. If python3 is missing or the emit script throws, grep checks on a non-existent or empty file fail with unhelpful errors. | Makefile cell8-certify: `@python3 tools/emit-earl-report.py > cell8-final-assertion-report.ttl` — no guard before this line. | Add python3 availability check at start of cell8-certify: `@command -v python3 >/dev/null || (echo "ERROR: python3 required" && exit 1)`. | NO |
| GAP_024 | verify-receipts in make check/adversarial attests hash integrity of ggen-generated artifacts (src/cmds/generated.rs, supabase/migrations/*, apps/mobile/*) but not their behavioral correctness. No gate verifies these artifacts are functionally consistent with what cargo compiled. | verify-receipts.sh: checks b3sum against .ggen/receipts/latest.json. No compilation or behavioral test of the verified files. | Documentation gap. Add comment in verify-receipts.sh and Makefile stating that verify-receipts attests hash integrity only, not behavioral correctness. | NO |

---

## Fix Priority Order

1. **GAP_001** — Add rust-toolchain.toml (channel = "nightly") to /Users/sac/open-ontologies. This is the single blocker for all cargo/make invocations. Zero surgery, five-minute fix.
2. **GAP_003** — Reconstruct nehemiah-52.ttl from docs/NEHEMIAH_52_OPERATING_GRAMMAR.md and BIBLE_O_STAR_002/003.md. This unblocks GAP_006, GAP_012, GAP_016 as side effects.
3. **GAP_004** — Recompute all four BLAKE3 hashes with b3sum; update receipt-chain.ttl; re-seal with Ed25519. Requires GAP_003 to be done first.
4. **GAP_005 + GAP_007** — Rewrite emit-earl-report.py to consume cargo test JSON output and emit real earl:passed/earl:failed assertions using the canonical urn:ontostar:gate: namespace.
5. **GAP_009** — Add deny-path tests for A1–A8 in cell8_thirteen_gates.rs.
6. **GAP_008** — Add nightly probe to verify-setup.sh.
7. **GAP_010** — Commit dirty files (Cargo.lock, Makefile, etc.) after toolchain fix.
8. **GAP_011** — Fix -o → -e in all pyshacl invocations.
9. **GAP_013 + GAP_014 + GAP_015** — Close adversarial test gaps.
10. **GAP_017** — Add receipt-chain.ttl hash verification to validate_bible_o_star.sh.
11. **GAP_019** — git add and commit the ontology/ directory.
12. **GAP_020 through GAP_024** — Minor polish and documentation gaps.

---

## Honest Re-verdict: BLOCKED

make cell8-certify cannot run because cargo build fails before the first source file compiles (GAP_001). Even if the toolchain were fixed, the EARL report would be manufactured by a hardcoded stub that certifies nothing (GAP_005), the primary ontology is corrupted text rather than valid Turtle (GAP_003), two of four BLAKE3 receipt hashes are stale (GAP_004), 10 of 15 SHACL validations fail (GAP_006), and A1–A8 deny paths are untested (GAP_009). The system cannot be certified ALIVE or even PARTIAL — it is BLOCKED at every layer of the certification stack.
