apiVersion: v1
kind: Service
metadata:
  name: ontostar
  namespace: ontostar
  labels:
    app.kubernetes.io/name: ontostar
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ontostar
  ports:
    - name: mcp-http
      port: 3050
      targetPort: 3050
      protocol: TCP
