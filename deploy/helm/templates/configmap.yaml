apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ontostar.fullname" . }}-config
  labels:
    {{- include "ontostar.labels" . | nindent 4 }}
data:
  config.toml: |
{{ .Values.config | indent 4 }}
