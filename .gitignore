/target
.worktrees/
data/
!case-studies/heritage-aerial/data/
!case-studies/heritage-aerial/data/**
!tests/data/
!tests/data/**
# Ignore generated OWL files in root, but keep benchmark references
/*.owl

# Secrets — Invariant 7. NEVER commit anything matching these patterns.
.env
.env.*
*.pem
*.key
secrets/

# Local Claude Code config — user-specific.
.claude/

# Local OntoStar working directories not part of the manufactured solution.
cell8-ggen/
ontology/cell8-*.ttl

# ggen pipeline runtime state — regenerable cache and current sync state.
# `.ggen/receipts/` is left tracked: it is the project's audit trail.
.ggen/cache/
.ggen/sync-state.json

# Source-code backup files (e.g. *.rs.backup left by code generators).
*.rs.backup

# Erlang / AtomVM build outputs.
*.beam
*.avm

# Editor / OS noise.
.DS_Store
*.swp
*.swo

# Backup files left by tools.
*.backup

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript
*.tsbuildinfo

# DuckDB database files.
*.duckdb

# Actuation temporary artifacts
artifacts/actuation/plans/*.json
!artifacts/actuation/plans/test_*.json
artifacts/ghf/ocel/observed.ocel.jsonl
.ggen/receipts/sync-*.json
artifacts/ghf/fleet/
artifacts/cli/JTBD-001-autonomic-ghf-verification.md
